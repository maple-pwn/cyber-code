# 安全政策

## 报告安全问题

请不要在公开 Issue、PR、日志或聊天室中披露未修复的漏洞、凭据、真实目标地址或完整敏感响应。优先使用 GitHub 私密漏洞报告；如果仓库未启用该功能，请联系维护者 `maple-pwn`，并提供：

- 受影响的 commit、版本或平台；
- 最小可复现步骤和必要配置；
- 影响、触发条件和建议修复；
- 已脱敏的日志、Tool Receipt 或 Evidence。

维护者会尽快确认收到报告，并在修复或缓解方案可用后更新报告状态。请给维护者合理的协调披露时间，不要在修复前公开 PoC 或目标数据。

## 授权测试边界

cyber-code 可以调用 Shell、网络、MCP、插件和安全工具。只对你拥有或明确获准测试的目标使用这些能力；默认 Permission Broker、Scope 审查、沙箱和 headless 拒绝行为是安全边界的一部分，不应通过提示词绕过。

`--permission-mode bypass`、真实 Runtime Source、Nuclei/Semgrep 以及云 Provider 都必须在隔离环境中显式启用。项目不为未经授权的扫描、破坏性操作、凭据窃取或规避第三方控制提供支持。

## 凭据和数据处理

凭据应通过环境变量、系统凭据存储或 CLI 配置引用的环境变量名提供，不要写入 YAML、Issue、fixture 或提交历史。测试日志应脱敏；报告中的 Evidence 仅保留完成复现所需的最小内容。
